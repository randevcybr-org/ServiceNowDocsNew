---
title: Add APIs
description: All the selected capabilities are listed as tabs on the Add APIs page. Add at least one API for each capability.
locale: en-US
release: australia
topic_type: task
last_updated: "2026-03-12"
reading_time_minutes: 3
breadcrumb: [Add a integration, Use, LLM-powered SIR integration builder, Security Operations]
---

# Add APIs

All the selected capabilities are listed as tabs on the Add APIs page. Add at least one API for each capability.

## Before you begin

**Note:**

-   The APIs uses Now Assist skills for code generation and for writing scripts. By default, these skills use Azure Open AI LLM \(Large Language Model\). If necessary, you can change the LLM by accessing the Now Assist Skill Kit application. The following LLMs are available: Azure Open AI, Now LLM.
-   Now assist for Spoke generator is used to generate flow action which uses Now LLM.

Role required: sn\_si\_int\_kit.integration\_creator

## Procedure

1.  Select the capability for which you want to add APIs.

2.  To add a new API, select **Add new API**.

3.  Select **API type**.

<table id="choicetable_g42_4hf_ngc"><thead><tr><th align="left" id="d70734e91">

Option

</th><th align="left" id="d70734e94">

Description

</th></tr></thead><tbody><tr><td id="d70734e100">

**Choose new API**

</td><td>

1.  Select the **API name**.

The following details of the selected APIs are displayed:**API documentation**, **API response structure**, **API sample response**.

2.  Select **Create API endpoint details**.

A flow designer action is generated.

3.  In the Flow designer action section, select the ![](../images/browse.png)icon to review the Flow Designer action generated by Now Assist. If necessary, you can update the values.
4.  Select one of the **API classification**.
    -   **Pre processing API**: Refers to the initial API calls required by an integration before making the primary API call to obtain the final results. These preliminary calls are essential for tasks such as retrieving necessary identifiers \(such as host ID\), fetching relevant information \(such as OS name\), or establishing a connection \(such as starting a session with the host\). By completing these preparatory steps, the preprocessing API enables the integration to successfully execute the main API call and retrieve the desired outcome.
    -   **Results API**: Delivers the final data, while the preprocessing APIs provides the necessary dependencies.
5.  Select **Generate Invocation Script** to generate the invocation script.

You can modify the script if necessary.

**Note:** Verify that the modified script is validated.

6.  Select **Generate Parsing Script** to generate the parsing script.

You can modify the script if necessary.

**Note:**

    -   Verify that the modified script is validated.
    -   If the generated parsing script isn’t correct and truncated, increase output token limit for the Code generation skill from Now Assist skill kit.
7.  To add multiple APIs, select the + icon and repeat from step 2.


</td></tr><tr><td id="d70734e196">

**Choose from existing API**

</td><td>

1.  In **Choose API**, select the API.

The API details are listed: **API documentation**, **API response structure**, **API sample response**.

2.  Select **API classification**.
    -   **Pre processing API**: The initial API calls required by an integration before making the primary API call to obtain the final results. These preliminary calls are essential for tasks such as retrieving necessary identifiers \(such as host ID\), fetching relevant information \(such as OS name\), or establishing a connection \(such as starting a session with the host\). By completing these preparatory steps, the preprocessing API enables the integration to successfully execute the main API call and retrieve the desired outcome.
    -   **Results API**: Delivers the final data, while the preprocessing APIs provides the necessary dependencies.
3.  Select **Generate Invocation Script** to generate the invocation script.

You can modify the script if necessary.

**Note:** Verify that the modified script is validated.

4.  Select **Generate Parsing Script** to generate the parsing script.

You can modify the script if necessary.

    -   Verify that the modified script is validated.
    -   If the generated parsing script isn’t correct and is truncated, increase output token limit for the Code generation skill from Now Assist skill kit.
5.  To add multiple APIs, select the + icon and repeat from step 2.


</td></tr></tbody>
</table>4.  To add multiple APIs, select the + icon and repeat from step 2.

5.  Select **Mark as complete**.

    At least one API must be configured as a Results API for each capability.

    All the APIs are listed for the selected capability.

6.  Select **Next** to review and publish the application.


